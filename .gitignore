# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agents configuration dirs — except .claude/settings.json, which carries the
# shared tree-watch hooks and belongs in a diff (coo#55). The directory itself
# must stay un-excluded: git cannot re-include a file under an excluded one.
.claude/*
!.claude/settings.json
.codex
